{
  "name": "reverb",
  "version": "0.1.0",
  "description": "Open Source people management tool, built in the open for leaders and people to safely have efficient and impactful conversations",
  "private": true,
  "packageManager": "pnpm@8.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^1.10.0"
  },
  "engines": {
    "node": ">=16",
    "pnpm": ">=8"
  }
}